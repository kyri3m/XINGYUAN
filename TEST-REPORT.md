# 全功能验收测试记录

测试日期：2026-09-06。对象：本机 `http://127.0.0.1:8080`。使用真实浏览器交互、隔离数据库 API 回归、前端逻辑测试及下载文件解析组合验证。

## 结果

- 后端：**26 项自动化测试通过**。
- 前端：**12 项逻辑断言通过**，全部 JavaScript 模块语法检查通过。
- 运行服务：登录、页面及静态资源、健康检查和核心 API 均通过检查。
- Chrome 导出的 CSV 已真实落盘，解析结果为 **13 条测试任务、9 列**，中文编码与 CSV 格式正确。
- 内置浏览器已实际验证小屏导航、桌面页面、对话框、表单、任务全流程。内置浏览器没有返回下载完成事件，下载落盘使用 Chrome 验证。

## 功能覆盖

| 功能 | 验证方式和结果 |
| --- | --- |
| 登录与退出 | 页面实测错误密码提示、管理员/组长/组员登录、退出，通过 |
| 注册邀请 | 页面实测复制邀请链接；API 实测注册码注册成功、单次使用、无效/停用代码、重置代码，通过 |
| 修改密码 | API 实测原密码错误、新密码生效；没有修改用户的真实管理员密码 |
| 新建/编辑任务 | 页面实测编号、企业、检测类别、复合有效期、工时、联系人、地址、经理、备注保存回填，通过 |
| 地区联动 | 页面实测合肥市→蜀山区选项与保存，通过 |
| 输入校验 | 页面实测检测项目格式、日期倒置、异常原因；API 覆盖重复编号、非法状态、字段、日期和工时，通过 |
| 任务搜索/筛选 | 页面实测关键词、状态、地区、有效期和重置，通过 |
| 分页 | 13 条任务实测第一页 10 条、第二页 3 条；筛选后页码回到有效范围，通过 |
| 任务详情 | 实测所有业务字段、检测项目、采样记录；恶意 HTML 以文字呈现，通过 |
| 派单/改派 | 页面实测组长+组员分配、改日期、取消后改派、人员冲突拦截，通过 |
| 日历 | 页面实测跨日任务、点击详情、月份前后切换、选日、返回今天，通过 |
| 采样记录 | 管理员与组员页面实测点位/文字记录保存及详情读取，通过 |
| 状态结果 | 页面实测完成、异常、取消与实际工时；组长仅能更新执行结果，通过 |
| 工作台 | 实测统计与任务状态一致、团队负载、指定日期任务；取消任务不计入当日采样安排，通过 |
| 人员管理 | 页面实测组长/组员新增、资料编辑、所属组长、邀请；API 实测停用、关联保护、删除，通过 |
| 账号管理 | 页面实测列表、编辑回填、角色模板切换、资料保存；API 实测创建、细粒度授权、停用、删除，通过 |
| 权限隔离 | 组员页面只显示本人任务、没有新建/编辑/派单/结果按钮；组长无任务编辑权；API 实测越权与精确 ID 匹配，通过 |
| 删除 | 页面检查确认/取消流程；隔离数据库实测正常删除及关联数据拦截，通过 |
| 导出 | Chrome 实际点击下载，解析文件；逻辑测试覆盖引号、逗号、换行与公式注入，通过 |
| 启停脚本 | 实际停止、后台重启、健康检查，通过 |

## 测试中发现并修复

1. 日历“今天”只切换月份，没有恢复当天的任务筛选。
2. 用户账号停用没有同步人员状态。
3. 修改人员普通资料可能恢复已停用账号。
4. 人员角色/状态入口可以绕过管理员账号保护。
5. 未排期的季度任务遗漏于季度待派单统计。
6. 月份筛选接受非法格式，日期接受非标准紧凑格式。
7. 任务恢复正常后仍展示旧异常/取消原因。
8. 工作台把已取消任务计入当天采样安排。
9. 工时缺少 NaN/Infinity 防护，CSV 公式防护未覆盖前置空白。
10. 删除确认按钮文案为“保存”，已改为“确认删除”；静态资源增加缓存重新验证。

## 数据清理与边界

测试任务及对应采样记录已按本地记录的 ID 清单清理，保留与用户自建数据存在关联的记录，避免破坏已有关系。测试账号及清理详情仅保存在本地，不纳入源码仓库。

这是当前实现功能的验收，不代表所有输入组合、长时间运行或并发负载都已穷尽。密码更改、注册与永久删除的提交分支主要在隔离 API 测试中验证；没有通过浏览器修改真实管理员凭据或删除用户数据。报告/实验室完整业务、照片上传和法定节假日并非当前实现功能，不列为通过项目。

复跑：`./.venv/Scripts/python.exe -m pytest -q` 和 `node tests/ui.mjs`。
