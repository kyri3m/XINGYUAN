# 项目审阅与改造记录

审阅源码：`https://github.com/kyri3m/XINGYUAN` main，获取于 2026-09-06。原始快照保留在 `upstream/XINGYUAN-main`；当前环境的 Git HTTPS helper 缺失，使用 GitHub 官方源码压缩包获取，因此此目录没有上游 Git 历史。

## 已修复问题

| 原问题 | 处理 |
| --- | --- |
| 普通登录用户可以修改任意任务大部分字段 | 统一任务修改授权；组长只能更新自己负责任务的执行结果 |
| 人员 1 可被错误匹配到人员 11 的任务 | 使用分隔后的 ID 集合精确判定，移除用户 ID 回退 |
| 异常状态的原因检查永远无法触发 | 所有状态统一校验，异常与取消原因必填 |
| 固定 JWT 签名密钥 | 本机首次生成随机密钥并持久化，支持环境变量覆盖 |
| 用户管理可越权创建管理员 | 非管理员禁止管理管理员、授予自身不具有的权限 |
| 注册码对所有用户暴露、中文人员角色注册后不匹配 | 非人员管理员隐藏注册码；角色标准化；停用人员不可注册 |
| 任意人员可为无关任务写入采样记录 | 采样录入验证任务存在及归属 |
| 重复编号、倒置日期、无效负责人、负数工时可入库 | 服务层统一校验，明确返回 409/422 |
| 派单没有人员冲突检查 | 同一人员在重叠采样日期的进行中任务上阻止重复派单 |
| 删除关联人员、任务或账号导致数据问题 | 对任务、组员和采样记录添加关联保护 |
| 前端直接拼接不可信字段 | 新前端统一转义动态文本，CSV 导出防公式注入 |
| 缺少依赖清单、启动文档中的模块路径错误 | 固定已安装依赖版本；统一从项目根目录启动 |
| 任务列表反复查询人员名称 | 每请求缓存人员映射与用户人员关联 |

## 架构与界面

后端分为配置、数据库会话、权限模板、领域模型、输入输出模型、共享业务服务和六组 API 路由。旧 API 路径保留，原数据库表结构保留，方便后续接入既有数据库。

前端采用原生 ES Modules，拆分为请求与会话、UI 基础组件、页面视图、业务表单、地区数据和应用状态。没有构建期依赖，静态模块由 FastAPI 同源提供。新版统一了登录、注册、工作台、任务列表、派单日历、人员管理与用户管理；包括任务筛选、分页、CSV 导出、多组长派单、采样记录、密码修改和注册邀请。

界面使用浅灰背景、白色面板、森林绿主色和自绘植物图形。支持小屏导航、响应式布局、键盘焦点、原生对话框、加载期间防重复提交和明确错误提示。新数据库只创建初始管理员，不写入模拟业务数据。

## 验证和范围

自动化验证包含 API 身份认证、精确任务归属、越权操作、派单冲突、输入校验、关联删除保护、注册码和密码更新，以及前端纯渲染和转义逻辑。后续已完成真实浏览器操作、桌面与小屏检查、Chrome 文件下载验证，详见 TEST-REPORT.md。

本次交付针对单机部署，绑定 `127.0.0.1:8080`。不包含开机自启、HTTPS 或公网服务。启动脚本后台运行，停止脚本验证进程身份。

当前边界：人员分配仍保留原 CSV ID 字段以兼容旧数据库；并发派单校验是应用级检查，大规模多人并发需要关联表迁移和事务锁。任务分页在客户端执行，超大数据量需要服务端分页。采样记录为点位和文字记录，没有照片文件上传。报告与实验室仍为权限模板，没有新增完整业务模块。日历显示周末，不请求外部节假日接口，未标注法定节假日及调休。修改密码不会即时注销其他已签发令牌，现有令牌有效期为 24 小时。

